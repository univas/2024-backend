### Instalar EJS
 - npm install ejs

### Configurar EJS
 - app.set('view engine', 'ejs')
 - app.set('views', './views')

### Criar Rota /contatos
 - Ler arquivo com os contatos salvos
 - Renderizar a view contatos passando os contatos lidos como parâmetros (dados)

### Criar View
 - Criar a view contatos dentro da pasta views
 - Usar o template <%%> para criar o código executável.